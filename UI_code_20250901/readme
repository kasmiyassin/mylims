Run the code with function:

the following code run the following command ` python3 app.py `.



# Updating the reverse proxy

<pre> ```bash git init git add . git commit -m "Initial commit" ``` </pre>

```bash git init git add . git commit -m "Initial commit" ``` 


```bash 
git init git add . 
git commit -m "Initial commit"
```
